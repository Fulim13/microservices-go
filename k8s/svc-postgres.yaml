apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    service: postgres
spec:
  # This name is the host inside DATA_SOURCE_URL for both order and payment.
  selector:
    service: postgres
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
      targetPort: 5432
